# ASSETS

This file tracks build and run outputs for the `riddim` project.

## Build Status

- [x] Initial bootstrap build (./gradlew build)

## Run Outputs

- [x] Quarkus app start
  ```
  ./gradlew :modules:app-api:quarkusDev
  ```
- [x] POST /sound/play response
  ```bash
  curl -X POST http://localhost:8080/sound/play \
    -H "Content-Type: application/json" \
    -d '{"id": {"value": "test-id"}, "type": "kick", "parameters": {"gain": 1.0}}'
  ```
  Response: `{"soundId":{"value":"test-id"},"status":"SENT","message":null}`

## Phase 2 Deliverables

- [x] Functional OSC Encoding (Real bytes sent to UDP)
- [x] Deterministic Scheduler (Priority queue based)
- [x] Configurable Engine (MicroProfile Config)
- [x] Sequence-aware DSL

### OSC Verification
Example encoded message for `/dirt/play "kick"`:
`2f 64 69 72 74 2f 70 6c 61 79 00 00 2c 73 00 00 6b 69 63 6b 00 00 00 00` (24 bytes)

### Scheduler Verification
`modules:playback:test` output:
`BUILD SUCCESSFUL` (Verified events triggered at 0.1 and 0.2 beats)

## Phase 4 Deliverables

- [x] Fedora 43 based Dockerfile for `app-api`
- [x] Docker Compose orchestration (`deployment/compose/docker-compose.yml`)
- [x] Makefile integration (`make docker-up`, `make docker-ps`)
- [x] Verified API response from container (HTTP 200 + SENT status)

## Phase 5 Deliverables

- [x] Comprehensive `.gitignore` implemented

## Module Artifacts

See [ASSETS_DASHBOARD.md](ASSETS_DASHBOARD.md) for the live build matrix and artifact details.

| Module                 | Primary Artifact                                                                  | Type     |
|------------------------|-----------------------------------------------------------------------------------|----------|
| `core`                 | `modules/core/build/libs/core-0.1.0-SNAPSHOT.jar`                                 | Library  |
| `model`                | `modules/model/build/libs/model-0.1.0-SNAPSHOT.jar`                               | Library  |
| `engine-supercollider` | `modules/engine-supercollider/build/libs/engine-supercollider-0.1.0-SNAPSHOT.jar` | Library  |
| `playback`             | `modules/playback/build/libs/playback-0.1.0-SNAPSHOT.jar`                         | Library  |
| `dsl`                  | `modules/dsl/build/libs/dsl-0.1.0-SNAPSHOT.jar`                                   | Library  |
| `app-api`              | `modules/app-api/build/libs/app-api-0.1.0-SNAPSHOT.jar`                           | Library  |
| `app-api`              | `modules/app-api/build/quarkus-app/quarkus-run.jar`                               | Runnable |

## Docs Artifacts

- Antora site output: `docs/build/docs-site` (generated by `npm run docs:build` in `docs/`).
