# TASK.riddim.001.root-gradle — Root Gradle Setup

## Goal
Initialize the Gradle wrapper and root build files.

## Actions
- Run `gradle wrapper`
- Create `settings.gradle.kts`
- Create `build.gradle.kts` (root)
- Verify `./gradlew` runs
