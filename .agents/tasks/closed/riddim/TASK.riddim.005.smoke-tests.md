# TASK.riddim.005.smoke-tests — Smoke Tests and Verification

## Goal
Verify the entire setup works end-to-end.

## Actions
- Run `./gradlew clean build`
- Run `curl` command to test the REST endpoint
- Update `ASSETS.md` with results
- Ensure `Makefile` is present and working
