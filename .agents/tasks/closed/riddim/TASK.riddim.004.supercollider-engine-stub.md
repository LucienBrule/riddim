# TASK.riddim.004.supercollider-engine-stub — Sound Engine and SuperCollider Stub

## Goal
Implement the core interfaces and a stubbed SuperCollider engine.

## Actions
- Define `ISoundEngine` in `modules:core`
- Define `SoundEvent` and `Receipt` in `modules:core` or `modules:model`
- Implement `SuperColliderSoundEngine` in `modules:engine-supercollider`
- Add basic OSC/UDP skeleton (placeholder encoder)
