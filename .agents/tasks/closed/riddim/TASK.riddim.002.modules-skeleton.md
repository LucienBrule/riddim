# TASK.riddim.002.modules-skeleton — Multi-module Skeleton

## Goal
Create the directory structure for all modules and their basic build files.

## Actions
- Create `modules/` directory
- Create subdirectories for: `core`, `model`, `engine-supercollider`, `playback`, `dsl`, `app-api`
- Create basic `build.gradle.kts` for each module
- Register modules in `settings.gradle.kts`
